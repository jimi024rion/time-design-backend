[project]
name = "time-design-backend"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "jimi024rion", email = "jimi024lab@gmail.com" }
]
dependencies = [
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/time_design_backend"]
